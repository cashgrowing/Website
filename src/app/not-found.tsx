import { Placeholder } from "@/components/Placeholder";

export default function NotFound() {
  return (
    <Placeholder
      title="That page has moved on"
      note="The link you followed does not point anywhere on the new site. The homes and the management pages are both a click away."
    />
  );
}
